# Clinic Management System (python3-app)

Учебный веб-проект на Python (Tornado), демонстрирующий работу CRUD-сервиса
для управления медицинской клиникой: больницы, врачи, пациенты, диагнозы
и связи между врачами и пациентами.

Проект используется для практики:
- рефакторинга кода с помощью LLM,
- автоматизированного тестирования,
- нагрузочного тестирования,
- контейнеризации и CI/CD.

---

## 🏗 Архитектура проекта

Проект реализован в виде монолитного веб-приложения с серверным рендерингом.

### Основные компоненты:
- **Python 3 + Tornado** — HTTP-сервер и обработчики запросов
- **Redis** — in-memory хранилище данных
- **HTML templates (Tornado templates)** — серверный рендеринг страниц
- **Locust** — нагрузочное тестирование
- **unittest + tornado.testing** — юнит-тесты
- **LLM (ChatGPT)** — генерация тестов, рефакторинг, документация

### Хранилище данных (Redis):
- `hospital:{id}` — информация о больнице
- `doctor:{id}` — информация о враче
- `patient:{id}` — информация о пациенте
- `diagnosis:{id}` — диагноз пациента
- `doctor-patient:{doctor_id}` — связь врач–пациент
- `*:autoID` — автоинкремент идентификаторов

---

## 🧹 Рефакторинг кода

Код приложения был полностью переработан с сохранением функциональности.

### Основные улучшения:
- устранено дублирование логики работы с Redis,
- упрощена обработка ошибок,
- улучшена читаемость и структура обработчиков,
- стандартизирована валидация входных данных,
- код приведён к единому стилю,
- повышена тестируемость.

Функциональность приложения сохранена и подтверждена прохождением всех юнит-тестов.

---

## 🚀 Запуск проекта

### 1️⃣ Требования
- Python 3.10+
- Redis
- virtualenv (venv)

---

### 2️⃣ Установка зависимостей

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
````

---

### 3️⃣ Запуск Redis

#### Через Homebrew (macOS):

```bash
brew install redis
brew services start redis
```

Проверка:

```bash
redis-cli ping
# PONG
```

---

### 4️⃣ Запуск приложения

```bash
export REDIS_HOST=localhost
export REDIS_PORT=6379
python main.py
```

Приложение будет доступно по адресу:

```
http://localhost:8888
```

---

## 🌐 API и эндпоинты

### Главная страница

* `GET /`

### Больницы

* `GET /hospital`
* `POST /hospital`
  **Параметры:** `name`, `address`, `beds_number`, `phone`

### Врачи

* `GET /doctor`
* `POST /doctor`
  **Параметры:** `surname`, `profession`, `hospital_ID`

### Пациенты

* `GET /patient`
* `POST /patient`
  **Параметры:** `surname`, `born_date`, `sex (M/F)`, `mpn`

### Диагнозы

* `GET /diagnosis`
* `POST /diagnosis`
  **Параметры:** `patient_ID`, `type`, `information`

### Связь врач–пациент

* `GET /doctor-patient`
* `POST /doctor-patient`
  **Параметры:** `doctor_ID`, `patient_ID`

---

## 🧪 Тестирование

Для тестирования используются:

* `unittest`
* `tornado.testing.AsyncHTTPTestCase`
* мокирование Redis

Запуск тестов:

```bash
cd python3-app
python -m unittest -v test_main
```

---

## ⚡ Нагрузочное тестирование

Для нагрузочного тестирования используется **Locust**.

### Запуск:

```bash
python -m locust -f locustfile.py --host http://localhost:8888
```

Web UI:

```
http://localhost:8089
```

### Параметры теста:

* 50 пользователей
* скорость роста: 5 пользователей/сек
* длительность: ~2 минуты

### Результаты:

* RPS: ~18
* Average latency: ~845 ms
* 95% latency: ~2800 ms
* Error rate: 0%

Приложение стабильно обрабатывает нагрузку без ошибок.

---

## 📦 Возможные улучшения

* Асинхронный Redis-клиент
* REST API вместо серверного рендеринга
* Авторизация и роли пользователей
* Горизонтальное масштабирование
* Вынос конфигурации в `.env`

---

## 📚 Назначение проекта

Проект используется в учебных целях для демонстрации полного цикла
улучшения проекта с использованием AI-ассистентов.